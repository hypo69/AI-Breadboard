# `SANDBOX` Directory — Experimental Prototypes

## Purpose
The `SANDBOX/` directory is reserved for experimental prototypes, parser debugging scripts, and temporary developer sandboxes.

> [!NOTE]
> Files in this directory are not part of the production release codebase and are excluded from production build pipelines.
