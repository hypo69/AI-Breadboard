# Windows Admin Toolkit Skill

This skill provides an interface to control the `windows_sysadmin` application within the AI Breadboard ecosystem.

## Features
- Active Directory (AD) management.
- User session monitoring and management.
- Windows security event auditing.

## Requirements
- `windows_sysadmin` must be running (usually on port 8100).
- Administrator privileges required for system management tasks.
