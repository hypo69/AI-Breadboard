# Tunnel Monitor Skill

This skill provides an interface to control the `cloudflared_monitor` application within the AI Breadboard ecosystem.

## Features
- Cloudflare tunnel health and status monitoring.
- Real-time log stream analysis.
- Endpoint latency probing.

## Requirements
- `cloudflared_monitor` must be running (usually on port 8104).
- Valid Cloudflare tunnel configuration.
